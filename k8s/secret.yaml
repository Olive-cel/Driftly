apiVersion: v1
kind: Secret
metadata:
  name: driftly-secrets
  namespace: driftly
  labels:
    app.kubernetes.io/part-of: driftly
type: Opaque
stringData:
  POSTGRES_USER: driftly
  POSTGRES_PASSWORD: driftly-secret
  POSTGRES_DB: driftly
  ANTHROPIC_API_KEY: "sk-ant-CHANGEME"
